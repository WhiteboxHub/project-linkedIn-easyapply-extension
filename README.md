Chrome Extension

Loads job list from easyapply_today.json

Requires selecting a Candidate and Employee

Performs Easy Apply automatically

Downloads one JSON file per run

Sends run summary to the server

Node.js Server

Receives run summary from extension

Stores run data in MySQL table job_activity_log

Uses job_id = 14 (fixed default)